{
  "name": "aws-metadata-sample",
  "version": "0.1.0",
  "bin": {
    "aws-metadata-sample": "bin/aws-metadata-sample.js"
  },
  "scripts": {
    "build": "echo \"The build step is not required when using JavaScript!\" && exit 0",
    "cdk": "cdk",
    "test": "jest"
  },
  "devDependencies": {
    "@aws-cdk/assert": "1.54.0",
    "aws-cdk": "1.54.0",
    "jest": "^25.5.0"
  },
  "dependencies": {
    "@aws-cdk/core": "1.54.0"
  }
}
